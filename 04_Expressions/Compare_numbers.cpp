# include <iostream>

using namespace std;

int main(){

    // x % 2 == 0 is x even?
    // x % 2! = 0 is x odd?

    // x % 5 == 0 is x a multiple of 5
    // 5 % x == 0 is x a factor of 5

    return 0;
}